# Priority 1 Fixes — Task Tracker

| # | Task | Status |
|---|------|--------|
| P1-1 | Delete `redact_key.sh` from repo root | `[ ]` |
| P1-2 | Read Chrome path from `CHROME_PATH` env in `BaseAgent.js` | `[ ]` |
| P1-3 | Hot-reload `answers.json` per agent run (not once at module load) | `[ ]` |
| P1-4 | Wrap LinkedIn stale page interactions in safe retry helper | `[ ]` |
| P1-5 | Form error recovery — detect errors, retry specific failed fields | `[ ]` |
