services:
  - type: web
    name: universal-job-agent
    env: node
    buildCommand: "./render-build.sh"
    startCommand: "node backend/server.js"
    envVars:
      - key: PUPPETEER_SKIP_CHROMIUM_DOWNLOAD
        value: true
      - key: NODE_ENV
        value: production
